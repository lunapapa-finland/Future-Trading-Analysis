name: finance_env
channels:
  - defaults
  - conda-forge  
dependencies:
  - python=3.9.6 
  - ipykernel
  - pip
  - pip:
      - yfinance
      - plotly
      - jinja2
      - markdown2
      - IPython
      - dash
      - opencv-python
      - cookiecutter
      - matplotlib
